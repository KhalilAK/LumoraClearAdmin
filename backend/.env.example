# Supabase dashboard -> Project Settings -> Data API / API Keys ("Connect" panel).
SUPABASE_URL=https://jbhvyonfilpmgpwadogn.supabase.co
SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImpiaHZ5b25maWxwbWdwd2Fkb2duIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjM4NTQzODYsImV4cCI6MjA3OTQzMDM4Nn0.j99hTF3BFxb2cFi3hbOTJ9rCbZR4vmnKFaHb09G22j8
# service_role / "secret" key — bypasses RLS, so this must ONLY ever live here
# (backend, gitignored .env), never in the frontend or a committed file.
SUPABASE_SECRET_KEY=your-secret-key
# Not currently used by the backend (login checks users.role directly, not
# Supabase Auth) — kept here in case that changes later.
SUPABASE_JWKS_URL=https://jbhvyonfilpmgpwadogn.supabase.co/auth/v1/.well-known/jwks.json

# Secret used to sign the admin session cookie. Any long random string.
SESSION_SECRET=

# Port the Express server listens on.
PORT=4000

# Origin the React dev server runs on, for CORS during local development.
FRONTEND_ORIGIN=http://localhost:5173

# GitHub personal access token with read access to KhalilAK/AppTest (it's
# private, so an unauthenticated request 404s) — powers the Logs page's
# GET /api/logs/updates commit feed.
GITHUB_API_KEY=
